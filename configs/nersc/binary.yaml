extends: strider_attention_full.yaml

project:
  name: binary
  output_dir: ${STRIDER_OUTPUT_DIR}/binary

reference:
  redshift_chunk_size: 24

training:
  # NERSC's longest-history forward/backward benchmark passed at batch 16
  # while retaining every observation and all 500 redshift candidates.
  batch_size: 16
  benchmark_batch_sizes: [8, 16]
  maximum_visits_per_batch: null
  maximum_squared_visits_per_batch: null
