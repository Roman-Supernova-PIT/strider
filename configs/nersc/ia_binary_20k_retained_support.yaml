extends: ia_binary_20k.yaml

project:
  name: ia_binary_20k_retained_support
  output_dir: ${STRIDER_OUTPUT_DIR}/ia_binary_20k_retained_support

data:
  prepared_dir: ${STRIDER_DATA_DIR}/ia_binary_20k_retained_support

observation:
  # This comparison keeps objects whose simulation template ends inside the
  # requested wavelength range. Training starts only if wavelength-support passes.
  template_support_policy: retain

onir:
  bank_path: ${STRIDER_DATA_DIR}/onir/ia_binary_20k_retained_support.npz
