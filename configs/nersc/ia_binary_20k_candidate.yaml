extends: ia_binary_20k_dense_dual.yaml

project:
  name: ia_binary_20k_candidate
  output_dir: ${STRIDER_OUTPUT_DIR}/ia_binary_20k_candidate

model:
  relative_brightness_evolution: true
  relative_brightness_initial_scale: 0.0
