extends: ia_binary_20k_roman_reference_learned.yaml

project:
  name: ia_binary_full_roman_reference_learned
  output_dir: ${STRIDER_OUTPUT_DIR}/ia_binary_full_roman_reference_learned

data:
  # Use the same complete prepared roles as the direct control so the encoder
  # comparison changes model capacity, not the training cohort.
  prepared_dir: ${STRIDER_DATA_DIR}/ia_binary_full
  max_objects:
    train: 0
    selection: 0
    calibration: 0
    test: 0
  training_sample_by_block: false
