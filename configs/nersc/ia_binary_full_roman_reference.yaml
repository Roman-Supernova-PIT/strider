extends: ia_binary_20k_roman_reference.yaml

project:
  name: ia_binary_full_roman_reference
  output_dir: ${STRIDER_OUTPUT_DIR}/ia_binary_full_roman_reference

data:
  # Scale the fixed-reference control to every source in the prepared flat-z
  # training and validation roles. The final Sundial test remains untouched.
  prepared_dir: ${STRIDER_DATA_DIR}/ia_binary_full
  max_objects:
    train: 0
    selection: 0
    calibration: 0
    test: 0
  training_sample_by_block: false
