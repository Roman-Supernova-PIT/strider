extends: ia_binary_20k_phase_control_main.yaml

project:
  name: ia_binary_20k_whole_spectrum_minimum_50
  output_dir: ${STRIDER_OUTPUT_DIR}/ia_binary_20k_whole_spectrum_minimum_50

model:
  # Preserve at least half of the learned whole-spectrum match. The current
  # unconstrained model is the matched control for this alias-focused test.
  dense_minimum_whole_weight: 0.50
  dense_initial_detail_weight: 0.25
