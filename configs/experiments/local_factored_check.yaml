extends: local_spectral_check.yaml

project:
  name: local_factored_check
  output_dir: runs/local_factored_check

model:
  architecture: factored_onir
  temporal_mode: spectral_evolution
  temporal_initial_scale: 0.0
  factored_attention_heads: 4
  factored_shape_initial_scale: 0.5
