extends: encoded_onir_named_clean.yaml

project:
  name: sundial_local_encoded_onir_development_test
  output_dir: runs/sundial_local_encoded_onir_development_test

data:
  runtime_object_limits:
    train: 400
    validation: 100
    test: 500

training:
  epochs: 8
  early_stopping_patience: 3
