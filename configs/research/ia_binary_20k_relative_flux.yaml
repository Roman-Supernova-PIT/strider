extends: ia_binary_20k_dense_dual.yaml

project:
  name: ia_binary_20k_relative_flux
  output_dir: ${STRIDER_OUTPUT_DIR}/ia_binary_20k_relative_flux

model:
  relative_brightness_evolution: true
  relative_amplitude_mode: object_normalized_flux
  relative_brightness_initial_scale: 0.5
