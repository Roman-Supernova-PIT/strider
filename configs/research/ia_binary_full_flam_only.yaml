extends: ia_binary_full_flam_anchor.yaml

project:
  name: ia_binary_full_flam_only
  seed: 73031
  output_dir: ${STRIDER_OUTPUT_DIR}/ia_binary_full_flam_only

training:
  # Deliberate full-FLAM endpoint of the matched 0/50/100% comparison.
  # Every source uses stored SNANA FLAM, while its paired blank remains an
  # independent controlled-background draw so FLAM - SIM_FLAM stays unseen.
  observed_flux_fraction: 1.0
