extends: ia_binary_20k_coadd_only.yaml

project:
  name: ia_binary_20k_coadd_denoise
  output_dir: ${STRIDER_OUTPUT_DIR}/ia_binary_20k_coadd_denoise

data:
  # Simulation-only supervision; measurement_inputs excludes this at runtime.
  include_clean_flux_target: true

model:
  coadd_reconstruction: true

training:
  coadd_reconstruction_loss_weight: 0.05
